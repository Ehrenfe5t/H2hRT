1：[mode]简短介绍production和debug模式什么区别？一个有控制台debug信息一个没有吗？2：[log_level]简短介绍log_level详细级别；3：[enable_console_logging]取消此参数是否可行合理？控制台常态化输出进度，稍后再明确debug信息等。4：[enable_file_logging]常态化输出log日志输出，取消此参数是否可行合理？解释为什么大场景会造成IO瓶颈；5：[worker_threads]是否可以实现设置为0时自动检测核心数并使用最大核心数，设置为非0值时根据设置值进行并行配置。6：[normalize_object_names]什么是规范化OBJ对象名？举例说明；7：[allow_name_auto_cleanup]作用是什么？8：[enable_wedge_build]常态化打开绕射棱边读取构建，取消此参数是否可行合理？9：[enable_scene_cache]常态化存储场景预处理结果，取消此参数是否可行合理？10：[enable_bvh_bruteforce_validation]是否还有必要？后续如果真的需要检查是否可以直接在项目里编写暴力检索代码文件硬编码是否使用？11：[bvh_leaf_size]是否有前沿算法或理论指导可以根据场景或配置进行自适应调整而非配置硬编码？12：[wedge_min/max_angle_deg]理论来说应该有建议值而不是作为自设配置参数，取消这两个参数是否可行合理？13：[filter_non_manifold_wedge_sources]同样UTD理论应该明确了此类边是否可以作为UTD有效棱边，取消此参数是否可行合理？14：[skip_coplanar_edges_for_wedge]作用是什么？15：[material_mapping_file]和scene_import.scene_material_map_file有什么区别？如果完全相同，取消该参数是否可行合理？16：[frequency_query_mode]理论上此类根据频点进行电参数插值是否合理？严格讨论，在我看来是不准确的，应该检查material_database_file中是否有设置的频率，如果没有则直接报错，重点讨论此项。17：[allow_material_fallback]什么情况会查询失败？我同样认为这样是不准确的，如果材质电参数对照表中没有此材质，也应直接报错。18：[default_background_medium]依附于17的讨论结果。19：[antenna\]天线相关配置统一在后续v6尾段或v7版本讨论，暂时不要影响主流程即可。20：所有交互类型通过数量决定，例如max_transmission_cout设置为0则代表不开透射仿真，从而取消对应的四种开关。21：[enable_mixed_path]当前是否还有保留必要？22：四个未使用的precise参数直接取消是否有影响？23：收发天线坐标是否可以将格式改为[tx-x,tx-y,tx-z][rx-x,rx-y,rx-z]此类更美观。24：当前precise是否缺失频率参数输入？无论是多频点还是单频点仿真都应该有这个入口吧？还是说放在了后面的em_solver计算时读取？25：是否支持少量多Rx输入（可以先不扩展此功能，给出结论即可）；26：coverage的四种交互的要求同precise，即取消交互开关而改用数量控制（0代表关）；27：[ray_power_threshold_linear]是否改为dB值更直观，例如使用40dB代表衰减40dB后停止（0.0001）10dB代表0.1，此类更直观的表达。此外precise模式没有衰减阈值是因为precise模式的寻径阶段不使用此阈值截断吗？28：[auto_grid_bounds]true的时候是全场景取点吗？如果是则无异议。29：[grid_margin_m]作用是什么，取消此参数是否合理可行？30：[tx_power_w]改为tx_power_dBm是否更直观，例如30dBm为1W。31：[store_paths]详细讨论其作用？用于可视化SBR？如果是必要的是否可以设置常开并针对性设计二进制或其他类型存储格式进行序列化反序列化等操作？32：[wedge_max_distance_m]我的理解是射线到一个点后查找这个范围内的所有楔边，只与此范围内的楔边进行碰撞检测？是否有保留必要？是否可以全场景楔边查找（采用针对性加速方法，可以先不实现仅作讨论）33：[wedge_sample_count]什么作用，绕射发生后沿Keller锥的绕射射线取样条数？34：[frequency_hz]precise和coverage共同使用？是否支持多频点或宽频带仿真(仅作讨论)；35：[enable_polarization]什么作用？电磁求解时是否考虑电磁波极化？precise模式下不应该永远考虑此效应吗？coverage下则可以粗略的进行功率上的简化计算；36：[output_directory]永远在统计目录下创建output文件夹作为根目录，取消此参数是否可行合理？37：其余输出内容和参数修改同样放在v6收尾或v7处理；38：数值容差此6个参数是否有理论建议值或者严格准确的自适应启发式设置？